# Local dev (docker-compose.yml) — optional, defaults already match these
DB_USER=inin
DB_PASSWORD=inin1234

# Production only (docker-compose.prod.yml, lives on the EC2 host as .env)
ECR_REGISTRY=<aws_account_id>.dkr.ecr.<region>.amazonaws.com
IMAGE_TAG=latest
